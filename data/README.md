# Data

Place eICU demo CSV files under `data/raw/eicu_demo/`.

Generated patient event streams, labels, encoded tensors, vocabularies, and
split metadata belong under `data/processed/eicu_demo/`. Both raw and processed
data directories are ignored by Git.

Do not commit raw eICU data or derived patient-level artifacts.

