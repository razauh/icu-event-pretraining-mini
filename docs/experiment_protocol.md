# Experiment Protocol

The experiment design follows a staged strategy rather than a full exhaustive
grid: representation ablations, pretraining comparisons, constrained Optuna
tuning, final verification, pseudo-client evaluation, FedAvg-style simulation,
and a rare-pattern memorisation probe.

